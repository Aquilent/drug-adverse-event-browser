@extends('layout')@section('content')  @include('includes.search-form')  <h2>Gender, Age, and Weight Differences in Drug Interactions</h2>    <p>The toxicity of a drug interaction may change due to gender, age, and weight. Read the categories below to see the differences in drug interactions.</p>    <table class="table table-striped table-hover">      <thead>        <tr>          <th>Gender</th>          <th class="text-right"># of Reports <a href="/instructions" title="About the Tool"><span class="glyphicon glyphicon-question-sign" aria-hidden="true"></span></a></th>        </tr>      </thead>      <tbody>        @foreach($genders AS $gender)        <tr>          <td>{{ format_title($gender->term) }}</td>          <td class="text-right">{{ $gender->count }}</td>        </tr>        @endforeach    </table>    <table class="table table-striped table-hover">      <thead>        <tr>          <th>Age</th>          <th class="text-right"># of Reports <a href="/instructions" title="About the Tool"><span class="glyphicon glyphicon-question-sign" aria-hidden="true"></span></a></th>        </tr>      </thead>      <tbody>        @foreach($ages AS $age)        <tr>          <td>{{ format_title($age->term) }}</td>          <td class="text-right">{{ $age->count }}</td>        </tr>        @endforeach    </table>    <table class="table table-striped table-hover">      <thead>        <tr>          <th>Weight</th>          <th class="text-right"># of Reports <a href="/instructions" title="About the Tool"><span class="glyphicon glyphicon-question-sign" aria-hidden="true"></span></a></th>        </tr>      </thead>      <tbody>        @foreach($weights AS $weight)        <tr>          <td>{{ format_title($weight->term) }}</td>          <td class="text-right">{{ $weight->count }}</td>        </tr>        @endforeach    </table>    <button type="button" class="btn btn-default btn-sm center-block btn-primary" data-link data-href="{{ route('listReactions', [ $drugOne, $drugTwo ]) . ((session('show') == 'all') ? '?show=all' : '') }}">BACK TO DRUG REACTIONS LIST</button>@stop