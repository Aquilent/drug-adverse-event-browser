@extends('layout')@section('content')  @include('includes.search-form')  <p class="text-right"><a href="{{ route('listReactions', [ $drug ]) }}" title="Return to Adverse Events list">&laquo; Return to Adverse Events list</a></p>  <h2><strong>{{ $drug }}</strong> Reported Interations</h2>  @if(!empty($interactions))    <p>Occurrences of <strong>{{ $reaction }}</strong> when taken with other drugs.</p>    <table class="table table-condensed table-striped">      <thead>        <tr>          <th>Taken with</th>          <th class="text-right">Occurrences</th>        </tr>      </thead>      <tbody>        @foreach($interactions AS $interaction)        <tr class='clickable-row' data-href='{{ route('listReactions', [ format_title($interaction->term) ]) }}'>          <td>{{ format_title($interaction->term) }}</th>          <td class="text-right">{{ $interaction->count }}</td>        </tr>        @endforeach    </table>    @if(app('request')->get('show') == 'all')      <button type="button" class="btn btn-default btn-sm center-block btn-primary" data-link data-href="{{ route('listInteractions', [ format_title($drug), format_title($reaction) ]) }}">SHOW TOP RESULTS</button>    @else      <button type="button" class="btn btn-default btn-sm center-block btn-primary" data-link data-href="{{ route('listInteractions', [ format_title($drug), format_title($reaction), 'show' => 'all' ]) }}">SHOW ALL RESULTS</button>    @endif  @else    <p>No Results Found, are you sure you spelled <strong>{{ $drug }}</strong> and <strong>{{ $reaction }}</strong> correctly?</p>  @endif@stop